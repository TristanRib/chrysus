# API (FastAPI) — fichier .env à la racine
GROQ_KEY=
API_ROOT_PATH=   # laisser vide en local, mettre /api sur Vercel

# Frontend (Next.js) — fichier frontend/.env.local
# NEXT_PUBLIC_API_URL=http://localhost:8000
